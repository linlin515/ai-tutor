# AI 助手 Android App — 产品需求文档 (PRD)

> 文档版本：v1.2（终稿）
> 日期：2026-05-15
> 负责人：PM
> 状态：终稿（待 CEO 确认）
> 包名：`com.aitutor.app`
> 目标平台：Android 8.0+ (API 26+)

---

## 目录

1. [产品概述](#1-产品概述)
2. [功能列表与优先级](#2-功能列表与优先级)
3. [用户角色](#3-用户角色)
4. [页面原型描述](#4-页面原型描述)
5. [用户交互流程](#5-用户交互流程)
6. [验收标准](#6-验收标准)
7. [异常场景处理](#7-异常场景处理)
8. [附录](#8-附录)
9. [AI学伴功能改进（基于竞品分析）](#9-ai学伴功能改进基于竞品分析)

---

## 1. 产品概述

### 1.1 产品定位

AI 学习助手 Android 应用，面向 K12 及大学生群体，提供：
- 智能问答与学科辅导
- 语音交互式学习
- 拍照解题（OCR + AI 分析）
- 多会话管理
- 个性化设置

### 1.2 核心目标

| 指标 | 目标值 |
|------|--------|
| 冷启动时间 | < 2s |
| 消息流式输出 | 不卡顿，首字延迟 < 500ms |
| SSE 连接成功率 | > 99% |
| ASR 识别准确率 | 离线 > 85%，云端 > 95% |
| 数据库 CRUD 响应 | < 50ms |
| 包体积增长 | 不超过 15MB (相比当前基线) |

### 1.3 约束条件

- **架构**：MVVM + Clean Architecture（三层架构），参考 `ARCH.md`
- **语言**：Kotlin
- **UI 框架**：Jetpack Compose (Material3)
- **DI**：Hilt
- **数据库**：Room (本地持久化)
- **网络**：Retrofit + OkHttp + SSE (ChatStreamApi)
- **导航**：Jetpack Navigation Compose
- **认证**：Bearer Token（自动刷新）

---

## 2. 功能列表与优先级

### 2.1 功能全景图

```
AI 助手 Android App
├── 认证模块 (P0)
│   ├── 手机号注册
│   ├── 手机号登录
│   └── Token 自动续期
├── 智能对话 (P0)
│   ├── 会话列表管理
│   ├── 聊天界面（气泡式消息）
│   ├── SSE 流式输出（打字机效果）
│   ├── Markdown / 代码块 / 公式渲染
│   ├── 多轮对话上下文保持
│   └── 本地历史记录（Room）
├── 多模态识别 (P0) ← 从 P1 升级
│   ├── CameraX 拍照上传解题
│   ├── 相册图片选择
│   ├── ML Kit 本地 OCR（实时文字检测）
│   ├── 云端 AI 分析
│   └── 全学科 OCR 识别（数学/物理/化学/生物等）
├── 自适应分步讲解 (P0) ← 新增
│   ├── 年级水平感知
│   ├── 分步推理链路
│   └── 多解法支持
├── AI 对话式教学增强 (P0) ← 新增
│   ├── 苏格拉底式引导提问
│   ├── 概念拆解与类比
│   └── 理解度检测
├── 语音交互 (P1)
│   ├── 长按录音 → ASR 转文字 → 发送
│   ├── 消息 TTS 语音朗读
│   └── 语音打断支持
├── 学习进度仪表盘 (P1) ← 新增
│   ├── 学习统计概览
│   ├── 知识点掌握度图谱
│   └── 学习趋势分析
├── 交互式测验 (P1) ← 新增
│   ├── AI 自动出题
│   ├── 在线作答
│   └── AI 批改与反馈
├── 间隔重复复习 (P1) ← 新增
│   ├── 错题本
│   └── 复习提醒
├── 用户设置 (P1)
│   ├── 模型选择切换
│   ├── 参数调整（Temperature / Top-P / Max Tokens）
│   ├── 主题切换（浅色 / 深色 / 跟随系统）
│   └── 清空对话 / 清除缓存
├── 游戏化 (P2) ← 新增
│   ├── 成就系统
│   ├── 连胜机制
│   └── 排行榜
├── 语音交互增强 (P2) ← 新增
│   ├── 识别率优化
│   └── 打断优化
├── 个人中心 (P2)
│   ├── 用户资料查看/编辑
│   └── 头像上传
└── 订阅管理 (P2)
    ├── 订阅状态查看
    └── 每日配额显示
```

### 2.2 详细功能列表

#### P0 — 核心对话（MVP，必须完成）

| ID | 功能 | 描述 | 依赖 |
|----|------|------|------|
| F01 | 手机号注册 | 输入手机号 + 密码完成注册，返回 Token | 后端 API |
| F02 | 手机号登录 | 输入手机号 + 密码登录，支持 Token 持久化 | 后端 API |
| F03 | Token 自动续期 | 401 时静默刷新 Token，刷新失败跳转登录 | TokenManager |
| F04 | 会话列表管理 | 显示所有历史会话，支持新建、切换、删除 | ConversationDao |
| F05 | 聊天界面 | 气泡式消息布局：左侧 AI，右侧用户 | ChatScreen |
| F06 | 文本消息发送 | 输入文本 → 发送 → 写入 Room → 请求 AI | ChatRepository |
| F07 | SSE 流式输出 | 逐块接收 AI 响应，实时追加显示 | ChatStreamApi |
| F08 | 打字机效果 | 流式文本逐字动画渲染 | StreamingText |
| F09 | Markdown 渲染 | 粗体/斜体/列表/表格/代码块/公式渲染 | MarkdownRender |
| F10 | 多轮对话上下文 | 发送历史消息列表保持上下文 | ChatRepository |
| F11 | 本地消息持久化 | 消息写入 Room，支持离线查看历史 | MessageDao |
| F12 | 消息发送状态 | 发送中/已发送/失败状态指示 | ChatUiState |
| F13 | 自动会话命名 | 首条消息截取前 20 字作为会话标题 | ConversationDao |
| F14 | 清空当前会话 | 删除当前会话所有本地消息 | MessageDao |
| F40 | 拍照解题增强 | 全学科拍照识别 + AI 流式分析解答（升级现有 F21） | CameraX, ML Kit, SolveApi |
| F41 | 自适应分步讲解 | AI 回复根据用户年级水平自动调整详细程度 | ChatApi (AI prompt engineering) |
| F42 | AI 对话式教学增强 | 苏格拉底式引导提问 + 概念拆解 + 理解度检测 | ChatApi (AI prompt engineering) |

#### P1 — 语音 + 多模态（增强体验）

| ID | 功能 | 描述 | 依赖 |
|----|------|------|------|
| F15 | 语音输入 (ASR) | 长按录音按钮 → Android SpeechRecognizer → 识别文本 → 自动发送 | VoiceRepository |
| F16 | 云端 ASR 备选 | 长音频 / 复杂场景降级到云端 ASR API | VoiceRepository |
| F17 | 消息语音朗读 (TTS) | 点击消息气泡 → TextToSpeech 朗读内容 | TextToSpeech |
| F18 | 云端 TTS 备选 | 长文本降级到云端 TTS API（流式播放） | VoiceRepository |
| F19 | 语音状态指示 | IDLE → LISTENING → PROCESSING → SPEAKING 状态切换 | VoiceUiState |
| F20 | 语音打断 | TTS 播放时可重新触发 ASR 打断 | VoiceInputBar |
| F21 | 拍照解题 | CameraX 拍照 → Multipart 上传 → AI 分析返回（基础版，由 F40 增强） | CameraScreen |
| F22 | 相册选择图片 | 从系统相册选取图片 → 上传分析 | PhotoPicker |
| F23 | 本地 OCR 实时检测 | ML Kit Text Recognition 实时检测取景框文字 | ML Kit |
| F24 | 图片消息显示 | 消息气泡中显示图片缩略图，点击预览 | MessageBubble |
| F25 | 模型选择切换 | 从后端获取模型列表 → 用户选择切换 | SettingsScreen |
| F26 | 参数调整 | Temperature (0-2)、Top-P (0-1)、Max Tokens 滑块调节 | SettingsScreen |
| F27 | 主题切换 | 浅色 / 深色 / 跟随系统 | SettingsScreen |
| F28 | 搜索历史对话 | 按关键词搜索本地消息记录 | MessageDao |
| F43 | 学习进度仪表盘 | 学习统计概览 + 知识点掌握度图谱 + 学习趋势分析 | AnalyticsEngine, Room |
| F44 | 交互式测验 | AI 自动出题 → 用户作答 → AI 批改 + 个性化反馈 | QuizApi, ChatApi |
| F45 | 间隔重复复习 | 错题本自动收藏 + 基于遗忘曲线的复习提醒 | SpacedRepetitionEngine, Room |

#### P2 — 体验完善

| ID | 功能 | 描述 | 依赖 |
|----|------|------|------|
| F29 | 用户资料编辑 | 修改昵称、年级、头像 | ProfileScreen |
| F30 | 订阅管理 | 查看订阅状态、每日配额、可用功能 | SubscriptionScreen |
| F31 | 清除缓存 | 清理图片缓存、非核心数据 | Settings |
| F32 | 新消息通知 | 后台收到新消息时本地通知 | NotificationManager |
| F33 | 会话列表排序 | 按更新时间倒序排列 | ConversationDao |
| F34 | Empty State | 无会话 / 无消息时的引导提示 | EmptyStateView |
| F35 | 错误重试 | 网络失败时显示重试按钮 | ErrorView |
| F46 | 游戏化 | 成就系统 + 连胜机制 + 排行榜 | GamificationEngine, Room |
| F47 | 语音交互增强 | ASR 识别率优化 + 语音打断流畅度优化 | VoiceRepository |

---

## 3. 用户角色

| 角色 | 描述 | 权限范围 |
|------|------|---------|
| **未登录用户** | 首次使用，无 Token | 仅能访问登录/注册页面 |
| **普通用户** | 已注册登录，免费套餐 | 基础对话 + 拍照解题（每日 5 次）+ 基础自适应讲解 |
| **订阅用户** | 付费订阅用户 | 无限制对话 + 所有功能 + 云端 TTS/ASR + 仪表盘/测验/复习/游戏化 |
| **会话参与者** | 与 AI 交互的终端用户 | 按会话管理消息 |

---

## 4. 页面原型描述

### 4.1 启动页 (SplashScreen)

- **URL 路由**：`splash`
- **布局**：居中显示 App Logo + 名称 "AI 学习助手"
- **逻辑**：
  - 检查本地 Token 是否存在且有效
  - Token 有效 → 自动跳转 `main`（首页聊天）
  - Token 无效 / 不存在 → 跳转 `login`
- **加载动画**：淡入淡出，2s 内完成

### 4.2 登录/注册页 (LoginScreen)

- **URL 路由**：`login`
- **布局**：顶部 Logo + 标语 → 手机号输入框 → 密码输入框 → 登录按钮 → 注册切换
- **用户操作**：
  - 输入手机号（11 位，自动格式化）
  - 输入密码（6-20 位，可见/隐藏切换）
  - 点击"登录" → POST `api/v1/auth/login` → 保存 Token → 跳转 `main`
  - 点击"注册" → 切换注册表单 → POST `api/v1/auth/register`
- **异常处理**：
  - 手机号格式校验：11 位数字
  - 密码长度校验：6-20 位
  - 网络错误：Toast 提示
  - 登录失败：显示后端返回错误信息

### 4.3 主页面 (MainScreen)

- **URL 路由**：`main`
- **布局**：底部导航栏 (BottomNavigation)，含 4 个 Tab
  | Tab | 图标 | 页面 |
  |-----|------|------|
  | 聊天 | 💬 (对话气泡) | ChatScreen |
  | 解题 | 📷 (相机) | CameraScreen |
  | 学习 | 📊 (仪表盘) | DashboardScreen (P1) |
  | 设置 | ⚙️ (齿轮) | SettingsScreen |
- **首次进入**：默认显示聊天 Tab
- **导航守卫**：未登录不可进入（重定向到 login）

### 4.4 聊天页面 (ChatScreen)

- **URL 路由**：`chat` / `chat/{conversationId}`
- **顶部栏 (TopAppBar)**：
  - 左侧：菜单按钮（≡），点击展开会话列表 BottomSheet
  - 中间：当前会话标题（自动命名 / 可自定义）
  - 右侧：新建对话按钮（+）
- **消息列表 (LazyColumn)**：
  - 气泡式布局：
    - 用户消息：右对齐，蓝色/主题色背景
    - AI 消息：左对齐，灰色/表面色背景
  - 消息类型：
    - 文本：支持 Markdown 渲染
    - 代码：代码块样式，有复制按钮
    - 图片：缩略图，点击全屏预览
    - 语音：音频波形图标，点击播放 TTS
  - 流式输出：打字机动画逐块渲染，光标闪烁指示
  - 滚动行为：新消息自动滚到底部，滑动查看历史时暂停自动滚动
  - **自适应分步讲解**：AI 回复开头标注年级适配标签，步骤可展开/折叠（P0）
  - **苏格拉底式教学**：AI 回复末尾附加引导问题，点击可继续对话（P0）
- **底部输入栏 (ChatInputBar)**：
  - 左侧：语音按钮（🎤）- 长按进入语音模式
  - 中间：文本输入框（多行，最大 2000 字）
  - 右侧：发送按钮（→ | 上箭头图标），无文本时禁用
  - 附加功能按钮：图片（📷），点击弹出选择：拍照 / 相册
- **状态指示**：
  - 消息发送中：气泡右侧显示旋转加载
  - 消息发送失败：气泡显示红色"!"，点击可重发
  - AI 思考中：显示 "AI 正在输入..." 动画
- **空状态**：无消息时显示欢迎语 + 示例问题卡片

### 4.5 会话列表侧边栏 (ConversationListSheet)

- **触发方式**：点击聊天页面左上角菜单按钮
- **布局**：BottomSheet 从底部弹出，占据 70% 高度
- **内容**：
  - 搜索框：按标题/消息内容搜索历史会话
  - 会话列表：标题 + 最后消息时间 + 消息数
  - 当前会话：高亮 + 指示器
  - 长按选项：重命名 / 删除
  - 滑动操作：左滑删除（带确认）
- **底部**：「新建对话」按钮
- **行为**：
  - 选择会话 → 关闭 Sheet → 加载该会话消息
  - 删除会话 → 级联删除所有消息（本地 Room）
  - 关闭 Sheet → 点击遮罩层或下滑

### 4.6 语音输入面板 (VoiceInputBar)

- **触发方式**：长按输入栏的语音按钮
- **布局**：底部浮现圆形录音面板
  - 中心：麦克风图标，随音量大小动画
  - 提示文字：「上滑取消」、「正在听...」、「识别中...」
  - 状态机：
    | 状态 | 显示 | 交互 |
    |------|------|------|
    | IDLE | 默认语音按钮 | 长按触发 |
    | LISTENING | 麦克风动画 + "正在听..." | 松开 → 提交 ASR |
    | PROCESSING | 加载动画 + "识别中..." | 等待 ASR 返回 |
    | SPEAKING | 扬声器动画 + "播放中..." | 点击打断 |
  - 取消手势：上滑到取消区域松开 → 放弃录音
  - 触发发送：松开按钮 → ASR 识别 → 文本填入输入框（或自动发送）
- **TTS 朗读**：点击消息气泡的扬声器图标 → 文字转语音播放

### 4.7 拍照解题页面 (CameraScreen)

- **URL 路由**：`solve`
- **布局**：
  - 全屏相机预览（CameraX）
  - 底部控制区：
    - 拍照按钮（圆形）
    - 相册选择按钮（最近一张图片缩略图）
    - 切换摄像头（前置/后置）
  - 顶部：返回按钮 + OCR 状态
  - OCR 覆盖层：ML Kit 实时显示取景框中检测到的文字区域（半透明边框）
  - **学科选择器**：拍照前可选学科（数学/物理/化学/生物/语文/英语），默认识别（F40）
- **拍照后流程**：
  1. 拍照 → 显示预览确认
  2. 确认 → Multipart 上传 → `POST /api/v1/solve/photo`
  3. 等待 AI 分析 → 显示 Loading
  4. 返回结果 → 自动跳转到聊天页面，AI 解答作为新消息追加
  5. **增强版**：AI 以流式（SSE）返回解题过程，逐步骤展示（F40）
- **相册选择**：通过 ActivityResultContracts.PickVisualMedia 选择图片
- **异常场景**：相机权限拒绝 → 提示引导开启；无相册权限 → 提示

### 4.8 学习进度仪表盘页面 (DashboardScreen) — P1 新增

- **URL 路由**：`dashboard`
- **布局**：顶部滚动卡片式布局 + 底部 Tab
  - **概览卡片**（顶部）：
    - 今日学习时长（分钟）
    - 今日解题数 / 正确率
    - 连续学习天数（连胜天数）
    - 总知识点掌握度（百分比环图）
  - **知识点掌握度图谱**（中间）：
    - 树状/网状知识图谱，每个节点代表一个知识点
    - 颜色编码：绿色（已掌握）、黄色（学习中）、红色（待加强）
    - 点击节点 → 查看该知识点的详细统计 + 相关错题
  - **学习趋势分析**（底部）：
    - 近 7 天/30 天学习曲线（折线图）
    - 维度：学习时长、解题量、正确率
    - 支持左右滑动切换时间粒度
- **数据来源**：
  - 本地：Room 数据库（解题记录、对话统计、错题记录）
  - 云端：`GET /api/v1/analytics/stats`（跨设备同步）
- **空状态**：首次使用时显示"开始学习吧！"引导，指向聊天/解题
- **刷新**：下拉刷新 + 页面自动刷新

### 4.9 交互式测验页面 (QuizScreen) — P1 新增

- **URL 路由**：`quiz`
- **进入方式**：从聊天页面 / 学习仪表盘 / 知识点图谱节点进入
- **布局**：
  - **顶部**：学科 + 知识点标签 + 题目数量（如"数学·二次函数 · 共 5 题"）
  - **题目区**：Markdown 渲染的题目文本，支持公式/图表
  - **选项区**：选择题 → 单选/多选按钮；填空题 → 文本输入框
  - **提交按钮**：全部作答完毕可提交
  - **结果区**（批改后）：
    - 每道题标注 ✅ / ❌
    - 正确答案显示
    - AI 对错误选项的解析（为什么错 + 正确思路）
- **交互流程**：
  1. 用户点击"生成测验" → 请求 `POST /api/v1/quiz/generate`（指定学科/知识点/难度）
  2. AI 生成 N 道题（默认 5 题，可调 3-10 题）
  3. 逐题展示 → 用户作答 → 全部完成提交
  4. 提交答案 → `POST /api/v1/quiz/submit` → AI 批改
  5. 结果逐题展示（批改 + 解析 + 建议）
- **异常场景**：生成超时 → 提示重试；提交失败 → 本地暂存，网络恢复后重试

### 4.10 间隔重复复习页面 (ReviewScreen) — P1 新增

- **URL 路由**：`review`
- **进入方式**：从学习仪表盘 / 主页进入
- **布局**：
  - **今日待复习**：显示今日需要复习的错题/知识点数量
  - **错题本列表**：按学科/知识点分组的错题卡片
    - 每张卡片：题目摘要 + 错误次数 + 最后错误时间 + 掌握度进度
    - 点击卡片 → 进入该题复习模式（重新作答 + AI 讲解）
  - **复习日历**：月度日历视图，标记有复习任务的日期
- **交互流程**：
  1. AI 对话/测验/解题中答错的题目自动加入错题本
  2. 后台算法根据遗忘曲线（EBM / SM-2 变体）计算下次复习时间
  3. 到达复习时间 → 发送本地通知"你有 N 道题待复习"
  4. 用户进入复习 → 展示题目（隐藏答案）→ 用户作答
  5. 作答后显示正确/错误 → 更新该题的复习间隔
  6. 连续 3 次正确 → 标记为"已掌握"，移出错题本（但仍可查阅历史）

### 4.11 设置页面 (SettingsScreen)

- **URL 路由**：`settings`
- **布局**：分组列表，Material3 Card 样式

**模型与参数 (P1)**
| 设置项 | 控件 | 说明 |
|--------|------|------|
| 模型选择 | 下拉选择器 | 列表来自 `GET /api/v1/models` |
| Temperature | 滑块 (0.0–2.0, 步进 0.1) | 默认 0.7 |
| Top-P | 滑块 (0.0–1.0, 步进 0.05) | 默认 1.0 |
| Max Tokens | 滑块 (256–4096, 步进 256) | 默认 2048 |

**主题与显示 (P1)**
| 设置项 | 控件 | 说明 |
|--------|------|------|
| 主题模式 | 单选 (浅色/深色/跟随系统) | 默认跟随系统 |

**语音 (P1)**
| 设置项 | 控件 | 说明 |
|--------|------|------|
| TTS 语速 | 滑块 (0.5–2.0) | 默认 1.0 |
| TTS 音色 | 下拉选择器 | 本地引擎可选 |

**通用 (P2)**
| 设置项 | 操作 | 说明 |
|--------|------|------|
| 清空对话 | 按钮 + 确认弹窗 | 删除所有本地会话和消息 |
| 清除缓存 | 按钮 + 确认弹窗 | 清理图片缓存等 |
| 版本号 | 只读显示 | `v1.0.0 (build 1)` |
| 关于 | 跳转 | 显示应用信息 |

### 4.12 个人中心页面 (ProfileScreen)

- **URL 路由**：`profile`（从设置进入）
- **布局**：
  - 顶部：头像（圆形，可点击更换）+ 昵称 + 年级
  - 信息卡片：手机号（只读）
  - 编辑按钮：可编辑昵称、年级
  - 退出登录：底部红色按钮 + 确认弹窗 → 清除 Token → 跳转 login

### 4.13 订阅页面 (SubscriptionScreen)

- **URL 路由**：`subscription`
- **布局**：
  - 当前计划卡片：免费版 / 订阅版
  - 配额进度：每日用量 N/5（免费版）
  - 功能对比表：免费 vs 订阅功能差异
  - 订阅按钮（跳转外部支付链接）

---

## 5. 用户交互流程

### 5.1 核心对话流程

```
用户打开 App
    │
    ├─ Token 有效? ─── 是 ──→ MainScreen (聊天 Tab)
    │                            │
    └─ 否 ──→ LoginScreen        │
                │               ├─ 已有会话? ── 是 ──→ 加载最新会话消息列表
                │               │                      │
                │               │                      ├─ 输入消息 → 发送
                │               │                      │   ├─ 消息写入 Room (状态: 发送中)
                │               │                      │   ├─ POST /v1/chat/completions (SSE)
                │               │                      │   ├─ 逐块接收流式响应
                │               │                      │   ├─ 逐块追加 UI + 写入 Room
                │               │                      │   └─ 流结束 → 消息状态: 完成
                │               │                      │
                │               │                      ├─ 拍照解题 → 见 5.3
                │               │                      │
                │               │                      └─ 语音输入 → ASR → 文字 → 发送
                │               │
                │               └─ 无会话 ──→ 空状态引导 → 新建会话 → 输入消息
                │
                └─ 登录成功 → 保存 Token → MainScreen
```

### 5.2 语音交互流程

```
聊天页面输入栏
    │
    ├─ 点击语音按钮 → 长按触发
    │   ├─ IDLE → 显示 VoiceInputBar
    │   ├─ LISTENING → 用户说话 → SpeechRecognizer
    │   │   ├─ 上滑取消 → 放弃录音 → IDLE
    │   │   ├─ 松开 → 提交 ASR 文字到输入框
    │   │   └─ 识别失败 → 提示"请重试" → IDLE
    │   ├─ PROCESSING → 云端 ASR 备选（长音频）
    │   └─ 识别完成 → 填充输入框并自动发送
    │
    └─ 点击消息 TTS 按钮
        ├─ 短消息 → 本地 TTS 引擎播放
        ├─ 长消息 → 云端 TTS 流式播放
        └─ 可点击其他消息打断当前播放
```

### 5.3 拍照解题流程

```
底部 Tab → 解题 (CameraScreen)
    │
    ├─ 选择学科 (可选，默认识别) ────→ 拍照预览
    │   ├─ 数学 → AI 数学引擎
    │   ├─ 物理 → AI 物理引擎
    │   ├─ 化学 → AI 化学引擎
    │   └─ 自动 → AI 全科识别
    │
    ├─ 拍照 → CameraX 拍照预览
    │   ├─ 确认 → Multipart 上传 → POST /api/v1/solve/photo
    │   ├─ 重拍 → 返回相机预览
    │   └─ 取消 → 返回
    │
    ├─ 相册选择 → 系统相册 → 选择图片 → 上传
    │
    └─ ML Kit OCR → 实时检测取景框文字（仅提示，不阻断拍照）

上传完成后：
    ├─ Loading 等待 AI 分析
    ├─ 成功 → 自动跳转聊天页面
    │   ├─ AI 以 SSE 流式返回解题过程
    │   ├─ 逐步骤展示 (步骤 1/N → 步骤 2/N → ...)
    │   ├─ 最终答案高亮
    │   └─ 可继续追问"为什么这一步要这样算？"
    └─ 失败 → 显示错误提示 → 可重试
```

### 5.4 会话管理流程

```
聊天页面
    │
    ├─ 新建会话 → 点击 "+" 按钮
    │   ├─ ConversationDao.insert() → 新会话
    │   ├─ 清空消息列表 → 输入框聚焦
    │   └─ 用户发出首条消息后自动生成标题
    │
    ├─ 切换会话 → 点击菜单 → 会话列表 BottomSheet
    │   ├─ 选择目标会话 → 加载该会话消息
    │   └─ 自动滚动到最新消息
    │
    └─ 删除会话 → 左滑 / 长按 → 确认弹窗
        ├─ ConversationDao.delete(id)
        ├─ 级联删除 Message
        └─ 如果删除当前会话 → 切换到最近会话 / 空状态
```

### 5.5 Token 续期流程

```
发起 API 请求
    │
    ├─ 响应 200 → 正常处理
    │
    └─ 响应 401 → TokenManager 检测
        ├─ 尝试刷新 Token: POST /api/v1/auth/refresh
        │   ├─ 刷新成功 → 更新本地 Token → 重试原请求
        │   └─ 刷新失败 → 清除 Token → 跳转 LoginScreen
        └─ 刷新请求本身 401 → 不重试 → 直接跳转登录
```

### 5.6 自适应分步讲解流程 — P0 新增

```
用户提问 / 拍照解题
    │
    ├─ 读取用户 Profile 中的年级信息（如"初三"）
    │   ├─ 无年级信息 → 默认使用标准分步讲解
    │   └─ 有年级信息 → 传递给 AI Prompt 作为上下文
    │
    ├─ AI 生成回复（带结构化的分步标记）
    │   ├─ 年级适配示例：
    │   │   ├─ 小学：步骤简单，每个步骤一句话 + 图示
    │   │   ├─ 初中：步骤中等，补充公式推导
    │   │   ├─ 高中：步骤完整，含理论推导
    │   │   └─ 大学：深入原理，含文献/扩展
    │   ├─ SSE 流式返回时，每个步骤作为独立块
    │   └─ 客户端根据步骤标记渲染为可折叠卡片
    │
    └─ 用户交互
        ├─ 点击展开/折叠某个步骤
        ├─ 对某步骤点"没看懂" → AI 自动补充更详细的解释
        └─ 切换难度级别 → AI 重新调整所有步骤的详细程度
```

### 5.7 AI 对话式教学（苏格拉底式）流程 — P0 新增

```
用户在聊天中提问
    │
    ├─ AI 检测为"学习型问题"（vs 简单问答）
    │   ├─ 是 → 进入苏格拉底式教学模式
    │   └─ 否 → 正常回答（保留退出教学模式的选项）
    │
    ├─ 苏格拉底式教学循环：
    │   1. 概念拆解 → 将问题分解为核心概念
    │   2. 引导提问 → AI 提问（非直接给答案）
    │      ├─ "你觉得这个问题中，哪些条件最重要？"
    │      ├─ "回忆一下勾股定理的公式是什么？"
    │      └─ "如果 x=0，这个等式会变成什么？"
    │   3. 用户回答 → AI 判断正确性
    │      ├─ 正确 → 认可 + 加深难度继续引导
    │      └─ 错误 → 提示思考方向 + 给出类比
    │   4. 逐步深入 → 直到用户找到答案/理解概念
    │   5. 总结归纳 → AI 给出完整解答 + 方法总结
    │
    ├─ 用户可随时退出教学模式 → 切换为直接回答模式
    │
    └─ 教学结束后
        ├─ AI 评估用户理解度（已掌握/需加强）
        ├─ 将知识点记录到学习进度（F43 数据源）
        └─ 建议相关练习题（F44 入口）
```

### 5.8 交互式测验流程 — P1 新增

```
用户进入测验模式
    │
    ├─ 选择学科 / 知识点 / 难度 / 题数
    │
    ├─ POST /api/v1/quiz/generate → AI 生成题目
    │
    ├─ 逐题展示 → 用户作答
    │   ├─ 单选题 / 多选题 / 填空题
    │   └─ 支持 Markdown 公式渲染
    │
    ├─ 全部作答 → 提交 POST /api/v1/quiz/submit
    │
    └─ AI 批改结果：
        ├─ 每题标注 ✅ / ❌
        ├─ 正确答案展示
        ├─ 错题解析（为什么选错了 + 正确思路）
        ├─ 掌握度评分（百分比）
        └─ 建议下一步学习方向
```

### 5.9 间隔重复复习流程 — P1 新增

```
用户学习过程中遇到错题
    │
    ├─ 自动加入错题本（本地 Room + 云端同步）
    │   ├─ 来源：对话中答错 / 测验答错 / 拍照解题答错
    │   └─ 记录字段：题目原文、正确答案、错误答案、错误时间、学科/知识点
    │
    ├─ 后台遗忘曲线引擎计算复习计划
    │   ├─ 首次复习：1 天后
    │   ├─ 二次复习：3 天后
    │   ├─ 三次复习：7 天后
    │   ├─ 四次复习：14 天后
    │   └─ 每次正确→间隔加倍；错误→间隔重置为 1 天
    │
    ├─ 复习时间到达 → 本地通知"你有 N 道题待复习"
    │
    └─ 用户进入复习
        ├─ 展示题目（隐藏答案）→ 用户作答
        ├─ 作答后对比正确答案
        │   ├─ 正确 → 更新间隔 + 掌握度提升
        │   └─ 错误 → 间隔重置 + 提供 AI 解析
        └─ 连续 3 次正确 → 标记"已掌握"→ 移出错题本
```

---

## 6. 验收标准

### 6.1 功能验收

#### P0 验收项

| ID | 验收项 | 条件 |
|----|--------|------|
| AC01 | 用户可注册新账号 | 输入有效手机号 + 密码 → 注册成功 → Token 保存 → 进入首页 |
| AC02 | 用户可登录已有账号 | 输入已注册手机号 + 正确密码 → 登录成功 |
| AC03 | Token 过期自动续期 | 发起请求返回 401 → 自动刷新（用户无感知）→ 重试成功 |
| AC04 | Token 刷新失败跳转登录 | 刷新请求返回 401 → 清除 Token → 跳转登录页 |
| AC05 | 输入文字发送消息 | 输入非空文本 → 点击发送 → 消息出现在气泡中（右侧） |
| AC06 | AI 流式回复 | 发送消息 → AI 回复逐字逐块出现（打字机效果） |
| AC07 | Markdown 正确渲染 | AI 回复含 `**粗体**`、`` `代码` ``、```代码块```、表格 → 正确渲染 |
| AC08 | 多轮对话上下文保持 | 连续发送多条消息 → AI 能引用上文内容回复 |
| AC09 | 本地消息持久化 | 发送消息 + AI 回复 → 杀死 App → 重新打开 → 消息仍在 |
| AC10 | 新建会话 | 点击 "+" → 新建空白会话 → 可发送新消息 |
| AC11 | 切换会话 | 点击菜单 → 选择历史会话 → 加载该会话消息列表 |
| AC12 | 删除会话 | 左滑/长按会话 → 确认删除 → 会话消失 → 消息级联删除 |
| AC13 | 会话自动命名 | 首条消息发出后 → 会话标题显示前 20 字 |
| AC14 | 消息发送状态 | 发送中显示加载 → 成功后正常 → 失败显示红色提示 |
| AC15 | 清空当前会话 | 清空操作 → 当前会话所有消息删除 → 显示空状态 |
| AC16 | 冷启动时间 < 2s | 从点击图标到进入首页（或登录页）< 2s |
| AC34 | 拍照解题-全学科识别 | 分别上传数学/物理/化学/生物的题目图片 → AI 正确识别学科并解答 |
| AC35 | 拍照解题-手写识别 | 拍照识别手写数学公式 → 准确率 ≥ 85% |
| AC36 | 拍照解题-流式返回 | 拍照上传后解题过程逐步骤以 SSE 流式返回 |
| AC37 | 自适应分步讲解-年级适配 | 分别用小/初/高/大学生账号提问同一知识点 → AI 回复详细程度递进 |
| AC38 | 自适应分步讲解-步骤可折叠 | AI 回复的每个步骤显示为可展开/折叠卡片 |
| AC39 | 自适应分步讲解-难度切换 | 点击"切换难度" → AI 重新生成适配当前年级的讲解 |
| AC40 | AI 对话式教学-引导提问 | 提问学习型问题 → AI 先反问引导思考，而非直接给答案 |
| AC41 | AI 对话式教学-理解度检测 | 教学结束后 AI 给出掌握度评估（已掌握/需加强） |
| AC42 | AI 对话式教学-退出机制 | 苏格拉底模式中用户可随时输入"直接给我答案"切换到直接回答 |

#### P1 验收项

| ID | 验收项 | 条件 |
|----|--------|------|
| AC17 | 长按录音 → ASR 转文字 | 长按语音按钮 → 说话 → 松开 → 文字出现在输入框 |
| AC18 | ASR 离线可用 | 断网状态下短句识别正常工作 |
| AC19 | 点击消息 → TTS 朗读 | 点击消息右侧喇叭图标 → 播放语音 |
| AC20 | 语音打断 | TTS 播放中 → 点击其他消息 → 打断当前 → 播放新的 |
| AC21 | 拍照上传解题 | 拍照 → 确认 → AI 返回结果 → 跳转聊天显示解答 |
| AC22 | 相册选择图片 | 从相册选图 → 上传 → AI 分析 |
| AC23 | ML Kit OCR 实时检测 | 相机取景框中文字区域有半透明高亮边框 |
| AC24 | 模型选择切换 | 设置中选择不同模型 → 后续对话使用新模型 |
| AC25 | Temperature 参数生效 | 调整 Temperature → 后续对话随机性变化可感知 |
| AC26 | 主题切换 | 设置中切换浅色/深色 → App 主题即时切换 |
| AC27 | MD 代码块复制 | 点击代码块右上角复制按钮 → 代码复制到剪贴板 |
| AC28 | 搜索历史对话 | 搜索框输入关键词 → 筛选出匹配的会话 |
| AC43 | 学习仪表盘-概览卡片 | 仪表盘显示今日学习时长、解题数、正确率、连续学习天数 |
| AC44 | 学习仪表盘-知识点图谱 | 知识点以树状/网状图展示，颜色区分掌握度（绿/黄/红） |
| AC45 | 学习仪表盘-趋势图 | 近 7 天学习曲线（折线图）包含学习时长/解题量/正确率 |
| AC46 | 交互式测验-生成题目 | 指定学科/知识点 → AI 生成 N 道题（默认 5 题） |
| AC47 | 交互式测验-提交批改 | 作答完成后提交 → AI 批改给出 ✅/❌ 及解析 |
| AC48 | 交互式测验-错题记录 | 答错的题目自动加入错题本 |
| AC49 | 间隔重复-自动收录错题 | 对话/测验/解题中答错题目自动加入错题本 |
| AC50 | 间隔重复-复习提醒 | 到达复习时间 → 本地通知提醒 |
| AC51 | 间隔重复-遗忘曲线 | 复习间隔按正确/错误自动调整（正确加倍/错误重置） |
| AC52 | 间隔重复-已掌握标记 | 连续 3 次正确 → 标记"已掌握" → 移出错题本 |

#### P2 验收项

| ID | 验收项 | 条件 |
|----|--------|------|
| AC29 | 编辑个人资料 | 修改昵称/年级 → 保存 → 页面更新 |
| AC30 | 查看订阅状态 | 显示当前套餐 + 配额剩余 + 功能列表 |
| AC31 | 退出登录 | 点击退出 → 确认 → 清除所有本地数据 → 跳转登录 |
| AC32 | 无会话空状态 | 首次使用 → 显示欢迎语 + 示例问题 |
| AC33 | 网络错误提示 | 断网发送消息 → 显示失败状态 → 点击重试 |
| AC53 | 游戏化-成就系统 | 达成特定条件（如首次解题/连续7天学习）→ 解锁成就徽章 |
| AC54 | 游戏化-连胜机制 | 连续学习天数自动计算，在仪表盘/主页展示 |
| AC55 | 游戏化-排行榜 | 全局/好友排行榜显示学习积分排名（每日更新） |
| AC56 | 语音识别率优化 | 在安静/中等噪声环境下 ASR 准确率 ≥ 90%（相比当前基线） |
| AC57 | 语音打断流畅度 | TTS 播放中触发 ASR → 中断延迟 < 300ms，无杂音 |

### 6.2 性能验收

| ID | 指标 | 目标值 | 测量方式 |
|----|------|--------|---------|
| PERF01 | 应用冷启动时间 | < 2000ms | `adb logcat -s ActivityManager:*`: Displayed |
| PERF02 | SSE 首字延迟 | < 500ms | 从发送到收到第一个 data chunk |
| PERF03 | 消息列表滑动流畅度 | 60fps | Profile GPU Rendering |
| PERF04 | Room 写入延迟 | < 50ms | Traceview |
| PERF05 | APK 体积增长 | ≤ 15MB | 对比当前基线 |
| PERF06 | 内存占用峰值 | ≤ 256MB | Android Profiler |
| PERF07 | 拍照解题端到端延迟 | ≤ 5s（从拍照到首字返回） | 计时 |
| PERF08 | 测验生成延迟 | ≤ 8s（5 题标准模式） | 计时 |
| PERF09 | 仪表盘数据加载 | ≤ 1s（首次）/ ≤ 200ms（缓存） | Traceview |

### 6.3 兼容性验收

| ID | 要求 |
|----|------|
| COMP01 | 支持 Android 8.0 (API 26) 及以上 |
| COMP02 | 支持 320dp ~ 600dp 屏幕宽度（手机） |
| COMP03 | 支持深色模式（跟随系统 + 手动设置） |
| COMP04 | 支持横竖屏旋转（聊天页面适配） |

---

## 7. 异常场景处理

### 7.1 网络异常

| 场景 | 表现 | 处理方式 |
|------|------|---------|
| 发送消息时无网络 | 消息发送失败 | 气泡显示红色"!"失败状态，点击可重试 |
| SSE 连接中断 | 流式输出停止 | 自动重连（指数退避 1s/2s/4s，最多 3 次） |
| SSE 重连失败 | 流式输出中断 | 显示"连接中断，请重试"提示 |
| 登录时网络超时 | 登录按钮保持加载态 | 15s 超时 → 显示"网络超时，请检查网络" |
| 图片上传中断 | 上传进度停止 | 显示断点重试按钮，支持重新上传 |
| 网络恢复 | - | NetworkMonitor 监听 → 自动恢复消息发送（待发队列） |
| 弱网（2G/3G） | SSE 延迟增加 | 显示加载动画，不自动失败，超时 30s |

### 7.2 API 错误码处理

| 错误码 | 含义 | 客户端表现 | 处理 |
|--------|------|-----------|------|
| 400 | 参数错误 | Toast "输入内容有误" | 检查输入合法性 |
| 401 | Token 过期 | 触发自动刷新 | TokenManager 静默刷新 |
| 403 | 配额不足 | 弹窗 "今日问答次数已用完" | 引导订阅/等待次日 |
| 404 | 资源不存在 | Toast "内容不存在" | 重新加载 |
| 429 | 频率限制 | 倒计时提示 "请 N 秒后再试" | 禁用发送按钮 N 秒 |
| 500 | 服务器错误 | Toast "服务器开小差了" | 自动重试 1 次 |
| -1 | 未知错误 | Toast "出错了，请稍后重试" | 记录日志 |

### 7.3 语音异常

| 场景 | 表现 | 处理方式 |
|------|------|---------|
| 麦克风权限拒绝 | 语音按钮灰色不可用 | 弹窗引导开启权限 |
| ASR 识别失败 | 语音识别无结果 | 提示"未识别到语音，请重试" |
| ASR 超时（10s 无声音） | 自动结束录音 | 提示"未检测到语音输入" |
| TTS 引擎未安装 | TTS 按钮不可用 | 提示"请安装语音引擎" |
| 录音时来电/通知 | 录音中断 | 自动保存已录部分，显示"录音被中断" |
| 同时触发多个 TTS | 声音重叠 | 取消当前播放 → 播放最新的 |

### 7.4 多媒体异常

| 场景 | 表现 | 处理方式 |
|------|------|---------|
| 相机权限拒绝 | 拍照页面提示无权限 | 弹窗引导开启 |
| 存储权限拒绝 | 相册不可用 | 降级为仅拍照 |
| 图片过大 (>10MB) | 上传前自动压缩 | 显示"正在压缩...", 压缩至 2MB 内 |
| 不支持图片格式 | 选择时过滤 | 仅显示 JPEG/PNG/WebP |
| OCR 未检测到文字 | 无边框高亮 | 不影响拍照上传流程 |
| 相册无图片 | 缩略图显示占位图 | 提示"去拍一张吧" |
| 拍照解题-AI 无法识别 | AI 返回"无法解析图像" | 提示重新拍照 / 选择更清晰图片 / 手动输入文字 |

### 7.5 数据异常

| 场景 | 表现 | 处理方式 |
|------|------|---------|
| Room 数据库损坏 | 应用闪退 | 启动时检测 → 自动重建数据库 |
| 本地数据版本不一致 | 字段不匹配 | 使用 `@Database(version=...)` + Migration |
| 消息内容超长 | 截断 | UI 显示 "消息过长已截断"，保留前 2000 字 |
| SQLite 磁盘满 | 写入失败 | 提示"存储空间不足，请清理后重试" |
| 并发读写冲突 | 写入失败 | Room 事务自动重试 |

### 7.6 UI/UX 异常

| 场景 | 表现 | 处理方式 |
|------|------|---------|
| 快速点击发送（连点） | 多次发送 | 发送按钮发送后立即禁用，直到收到首个 chunk |
| 后台切回 | 流式输出状态丢失 | ViewModel 保存 state → 恢复流式输出 |
| 横竖屏旋转 | 布局重排 | Compose 自动适配，消息位置回滚到旋转前 |
| 系统字体缩放过大 | 布局错乱 | 限制最大字体倍数为 1.3x |
| 键盘弹出/收起 | 输入栏被遮挡 | `imePadding()` → 输入栏跟随键盘 |
| 超长消息（>5000 字 AI 回复） | 列表卡顿 | LazyColumn 虚拟化，消息组件懒加载 |
| 数百条消息的历史会话 | 滚动卡顿 | 分页加载，每次加载 50 条 |

### 7.7 新功能异常场景

| 场景 | 表现 | 处理方式 |
|------|------|---------|
| 拍照解题-模糊图片 | AI 分析失败 | 提示"图片不够清晰，请重新拍照/选择清晰图片" |
| 拍照解题-非题目图片 | AI 返回"未检测到题目" | 提示用户拍照解题功能仅支持题目识别 |
| 自适应讲解-年级信息缺失 | 默认使用标准难度 | 在设置中引导用户填写年级信息以获得个性化体验 |
| 苏格拉底式教学-用户连续答错 | 教学陷入循环 | AI 主动提供提示或降级为直接讲解模式 |
| 测验生成超时 (>15s) | 生成失败 | 提示"题目生成超时，请重试"，提供简化模式选项 |
| 测验提交失败 | 数据丢失 | 本地缓存用户答案，网络恢复后自动提交 |
| 错题本数据量大 (>1000 题) | 加载缓慢 | 分页加载（每次 50 条），支持按学科过滤/搜索 |
| 游戏化-数据不同步 | 成就计数不准 | 本地优先 + 云端同步（最终以云端为准） |

---

## 8. 附录

### 8.1 术语表

| 术语 | 说明 |
|------|------|
| SSE | Server-Sent Events，服务端推送事件流协议 |
| ASR | Automatic Speech Recognition，自动语音识别 |
| TTS | Text-to-Speech，文本转语音 |
| ML Kit | Google 机器学习套件，提供端侧 OCR 等能力 |
| Clean Architecture | 三层架构：UI / Domain / Data |
| Room | Android 官方 ORM 持久化框架 |
| EventSource | OkHttp 的 SSE 事件源接口 |
| StateFlow | Kotlin 响应式状态容器，用于 ViewModel 状态管理 |
| DataStore | Android 官方键值对/偏好存储 |
| Socratic Teaching | 苏格拉底式教学法：通过引导性提问激发学生独立思考 |
| Spaced Repetition | 间隔重复：基于遗忘曲线的复习调度算法 |
| EBM (Expanding Retrieval) | 扩展性提取练习：间隔重复的一种变体 |

### 8.2 参考文档

| 文档 | 路径 | 说明 |
|------|------|------|
| 架构设计 | `ARCH.md` | Clean Architecture 三层架构、模块划分、数据流 |
| API 接口 | `API.md` | 后端 API 定义、请求/响应格式、错误码 |
| 项目结构 | `PROJECT_STRUCTURE.md` | 完整目录树、文件状态、待建清单 |
| 竞品分析 | `COMPETITIVE_ANALYSIS.md` | Photomath/Quizlet/Gauthmath 竞品分析与差距洞察 |
| CEO 审查 | `CEO_REVIEW.md` | CEO 文档审查结论与产品定位策略 |

### 8.3 版本历史

| 版本 | 日期 | 变更内容 | 负责人 |
|------|------|---------|--------|
| v1.0 | 2026-05-14 | 初版 PRD | PM |
| v1.1 | 2026-05-15 | 基于竞品分析新增 F40-F47，升级到 47 功能项；新增第 9 章 Feature Matrix | PM |
| v1.2 | 2026-05-15 | 终稿：修复章节编号，更新文档状态，确认完整内容 | PM |

---

## 9. AI学伴功能改进（基于竞品分析）

### 9.1 新增功能详细描述

#### P0 功能

---

##### F40 — 拍照解题增强

| 属性 | 内容 |
|------|------|
| **ID** | F40 |
| **优先级** | P0（核心差异化） |
| **功能名称** | 拍照解题增强 — 全学科 OCR + AI 流式分析 |
| **描述** | 基于现有 F21 拍照解题能力进行升级，从"仅拍照上传"扩展到"全学科智能识别 + 流式分步解答"。用户在 CameraScreen 拍照或选择图片后，系统自动识别学科（数学/物理/化学/生物/语文/英语）并引导至对应 AI 分析引擎，解题结果通过 SSE 流式逐步骤返回聊天页面。支持手写体识别和印刷体识别。 |
| **依赖** | F07 (SSE 流式输出), F21 (拍照解题基础版), F22 (相册选择), F23 (ML Kit OCR), SolveApi |
| **交互流程** | 见 5.3 节拍照解题流程（增强版） |
| **验收标准** | AC34: 全学科识别；AC35: 手写识别≥85%；AC36: 流式返回 ；PERF07: 端到端≤5s |

---

##### F41 — 自适应分步讲解

| 属性 | 内容 |
|------|------|
| **ID** | F41 |
| **优先级** | P0（核心价值主张） |
| **功能名称** | 自适应分步讲解 |
| **描述** | AI 回复内容根据用户 profile 中的年级信息自动调整讲解的详细程度和表达方式。AI 回复以结构化步骤块（Step 1/N, Step 2/N）的形式组织，客户端渲染为可展开/折叠的步骤卡片。用户年级越高，步骤越深入、专业术语越多；年级越低，步骤越简单、用词越通俗、图示越多。支持用户手动切换难度级别，AI 重新生成对应难度的讲解。 |
| **依赖** | F05 (聊天界面), F06 (文本消息发送), F07 (SSE 流式输出), F29 (用户资料-年级字段), ChatApi (prompt engineering) |
| **交互流程** | 见 5.6 节自适应分步讲解流程 |
| **验收标准** | AC37: 年级适配；AC38: 步骤可折叠；AC39: 难度切换 |

---

##### F42 — AI 对话式教学增强（苏格拉底式）

| 属性 | 内容 |
|------|------|
| **ID** | F42 |
| **优先级** | P0（差异化 + 真实学习价值） |
| **功能名称** | AI 对话式教学增强 — 苏格拉底式引导 |
| **描述** | 当用户提出"学习型问题"时，AI 不直接给出答案，而是通过苏格拉底式引导提问帮助用户自己推导出答案。教学过程包括：概念拆解 → 引导提问 → 用户回答 → 正确性判断 → 逐步深入 → 总结归纳。AI 在教学结束后评估用户理解度并记录到学习进度系统。用户可随时退出教学模式切换为直接回答。 |
| **依赖** | F05 (聊天界面), F06 (文本消息发送), F07 (SSE 流式输出), F43 (学习进度仪表盘-数据来源), ChatApi (prompt engineering) |
| **交互流程** | 见 5.7 节 AI 对话式教学流程 |
| **验收标准** | AC40: 引导提问；AC41: 理解度检测；AC42: 退出机制 |

---

#### P1 功能

---

##### F43 — 学习进度仪表盘

| 属性 | 内容 |
|------|------|
| **ID** | F43 |
| **优先级** | P1（留存关键） |
| **功能名称** | 学习进度仪表盘 |
| **描述** | 在 MainScreen 新增"学习"Tab（DashboardScreen），提供可视化的学习数据看板。包含三个模块：(1) 顶部概览卡片 — 今日学习时长、解题数/正确率、连续学习天数、总掌握度；(2) 知识点掌握度图谱 — 树状/网状知识图谱，颜色区分掌握度（绿/黄/红）；(3) 学习趋势分析 — 近 7 天/30 天学习曲线折线图。 |
| **依赖** | Room (解题记录、对话统计、错题记录), AnalyticsEngine, Tab 导航 |
| **交互流程** | 进入学习 Tab → 加载统计 → 展示概览卡片 → 可点击图谱节点查看详情 |
| **验收标准** | AC43: 概览卡片；AC44: 知识点图谱；AC45: 趋势图；PERF09: 数据加载≤1s |

---

##### F44 — 交互式测验

| 属性 | 内容 |
|------|------|
| **ID** | F44 |
| **优先级** | P1（对标 Quizlet 核心功能） |
| **功能名称** | 交互式测验 |
| **描述** | 用户指定学科/知识点/难度/题量，AI 自动生成测验题目（单选/多选/填空）。用户逐题作答后提交，AI 批改并给出每道题的 ✅/❌ 标注、正确答案展示、错因解析和建议。测验结果自动同步到学习进度仪表盘和错题本。支持在聊天页面/知识点图谱节点直接触发出题。 |
| **依赖** | ChatApi (AI 出题), F43 (学习仪表盘-数据来源), F45 (间隔重复-错题本), QuizApi |
| **交互流程** | 见 5.8 节交互式测验流程 |
| **验收标准** | AC46: 生成题目；AC47: 提交批改；AC48: 错题记录；PERF08: 生成≤8s |

---

##### F45 — 间隔重复复习

| 属性 | 内容 |
|------|------|
| **ID** | F45 |
| **优先级** | P1（对标 Quizlet 核心功能） |
| **功能名称** | 间隔重复复习（错题本 + 遗忘曲线） |
| **描述** | 用户在对话、测验、拍照解题中答错的题目自动收录到错题本。后台基于 EBM 变体（扩展性提取练习）的遗忘曲线算法计算每道题的复习计划。到达复习时间时通过本地通知提醒用户。复习时展示题目（隐藏答案），用户重新作答后对比正确答案，根据作答结果自动调整复习间隔（正确加倍/错误重置）。 |
| **依赖** | Room (错题数据), SpacedRepetitionEngine, NotificationManager, F44 (测验-数据来源) |
| **交互流程** | 见 5.9 节间隔重复复习流程 |
| **验收标准** | AC49: 自动收录错题；AC50: 复习提醒；AC51: 遗忘曲线；AC52: 已掌握标记 |

---

#### P2 功能

---

##### F46 — 游戏化

| 属性 | 内容 |
|------|------|
| **ID** | F46 |
| **优先级** | P2（体验增强） |
| **功能名称** | 游戏化 — 成就/连胜/排行榜 |
| **描述** | 建立游戏化激励体系，提升用户学习动力和留存率。包含：(1) 成就系统 — 预设成就徽章（如"首次解题""连续学习 7 天""答对 100 题""学霸"等），达成条件自动解锁；(2) 连胜机制 — 连续学习天数自动累计，在主页/仪表盘展示，中断重置；(3) 排行榜 — 用户学习积分（由学习时长/解题数/正确率等综合计算）排名，支持全局排行和好友排行。 |
| **依赖** | Room (学习数据统计), GamificationEngine, F43 (仪表盘-数据展示) |
| **交互流程** | 学习行为触发成就检测 → 达成条件 → 弹窗"成就解锁！" → 更新成就列表；每日打开 App → 连胜天数更新 |
| **验收标准** | AC53: 成就系统；AC54: 连胜机制；AC55: 排行榜 |

---

##### F47 — 语音交互增强

| 属性 | 内容 |
|------|------|
| **ID** | F47 |
| **优先级** | P2（蓝海差异化） |
| **功能名称** | 语音交互增强 — 识别率优化 + 打断优化 |
| **描述** | 基于现有 F15-F20 语音交互功能进行增强优化：(1) ASR 识别率优化 — 在噪声环境下通过云端 ASR 降级 + 多麦克风融合提升准确率，环境自适应降噪算法；(2) 语音打断优化 — TTS 播放中用户可随时通过语音触发新的 ASR 请求，打断延迟 < 300ms，播放平滑切换，无杂音/爆音。 |
| **依赖** | F15 (ASR), F16 (云端 ASR 备选), F19 (语音状态指示), F20 (语音打断) |
| **交互流程** | 用户 TTS 播放中 → 长按语音按钮 → 触发打断 → 旧 TTS 立即停止 → ASR 开始监听 → 识别后处理新请求 |
| **验收标准** | AC56: 识别率≥90%（噪声环境）；AC57: 打断延迟<300ms |

---

### 9.2 功能优先级总览（含竞品对标）

| 优先级 | ID | 功能 | 新增/升级 | Photomath | Quizlet | Gauthmath | 差异化价值 |
|--------|----|------|-----------|:---------:|:-------:|:---------:|-----------|
| **P0** | F40 | 拍照解题增强（全学科OCR+流式） | 升级 F21 | ✅ (仅数学) | ❌ | ✅ (仅STEM) | **多学科 > 竞品** |
| **P0** | F41 | 自适应分步讲解 | 新增 | ✅ (固定) | ❌ | ✅ (固定) | **自适应 > 固定步骤** |
| **P0** | F42 | AI 对话式教学增强（苏格拉底式） | 新增 | ❌ | Q-Chat (付费) | ❌ | **引导式教学 > 直接回答** |
| **P1** | F43 | 学习进度仪表盘 | 新增 | ❌ | 基础 | ❌ | **完整的可视化学情分析** |
| **P1** | F44 | 交互式测验 | 新增 | ❌ | ✅ (核心) | ❌ | **与自适应讲解/错题本联动** |
| **P1** | F45 | 间隔重复复习 | 新增 | ❌ | ✅ (核心) | ❌ | **与错题本 + 测验深度绑定** |
| **P2** | F46 | 游戏化 | 新增 | ❌ | ✅ (强) | ❌ | **学习积分综合排名** |
| **P2** | F47 | 语音交互增强 | 升级 F15-F20 | ❌ | ❌ | ❌ | **蓝海 - 无竞品具备** |

### 9.3 Feature Matrix（vs 竞品）

| 功能维度 | 本产品 (AI学伴目标) | Photomath | Quizlet | Gauthmath |
|---------|:------------------:|:---------:|:-------:|:---------:|
| **拍照解题 (Photo Solving)** | ✅ **全学科** (数学/物理/化学/生物/语文) | ✅ 仅数学 | ❌ | ✅ 仅STEM |
| **拍照解题 - 流式分步返回** | ✅ SSE 逐步骤流式输出 | ❌ 静态步骤 | ❌ | ❌ 静态步骤 |
| **自适应分步讲解**（按年级） | ✅ **自适应** (小学→大学自动调整) | ⚠️ 固定步骤 | ❌ | ⚠️ 固定步骤 |
| **AI 对话式教学**（苏格拉底式） | ✅ **引导式**（提问→思考→回答→总结） | ❌ | ⚠️ Q-Chat (问答式) | ❌ |
| **AI 理解度评估** | ✅ 教学结束后评估掌握度 | ❌ | ❌ | ❌ |
| **学习进度仪表盘** | ✅ **三维度**（概览/图谱/趋势） | ❌ | ⚠️ 按学习集基础统计 | ❌ |
| **知识点掌握度图谱** | ✅ 树状/网状可视化 | ❌ | ❌ | ❌ |
| **交互式测验（AI 出题）** | ✅ AI 自动出题 + 批改 | ❌ | ✅ 学习集→测试 | ❌ |
| **间隔重复复习** | ✅ **EBM 变体** + 错题本联动 | ❌ | ✅ Learn 模式 | ❌ |
| **错题本** | ✅ **全来源自动收录**（对话/测验/解题） | ❌ | ❌ | ❌ |
| **游戏化** | ✅ 成就 + 连胜 + 排行榜 | ❌ | ✅ ⭐ 强项 (Match/Gravity) | ❌ |
| **语音输入 (ASR)** | ✅ 已有 + 增强优化 | ❌ | ❌ | ❌ |
| **TTS 语音朗读** | ✅ 已有 + 增强优化 | ❌ | ✅ 文本转语音 | ❌ |
| **语音打断** | ✅ 优化 < 300ms | ❌ | ❌ | ❌ |
| **SSE 流式输出** | ✅ ✅ **核心能力** | ❌ | ❌ | ❌ |
| **Markdown/公式渲染** | ✅ ✅ **核心能力** | ❌ | ❌ | ❌ |
| **多学科覆盖** | ✅ 全学科 | ❌ 仅数学 | ✅ 通用（Flashcard） | ⚠️ 仅STEM |
| **多会话管理** | ✅ 已有 | ❌ | ❌ | ❌ |

### 9.4 竞品差距总结与行动建议

| # | 差距领域 | 优先级 | 竞品现状 | 我方定位 | 建议行动 |
|---|---------|:------:|---------|---------|---------|
| 1 | **拍照解题 (全学科)** | P0 | Photomath/Gauthmath 仅数学/STEM | **全学科 OCR** 是首要差异化 | 后端集成多学科 AI 分析引擎，前端 CameraX + ML Kit 全科识别 |
| 2 | **自适应分步讲解** | P0 | 竞品均为固定步骤 | **年级自适应** 是最直接的用户价值感知 | 后端 Prompt Engineering 加入年级上下文，前端步骤折叠组件 |
| 3 | **AI 对话式教学** | P0 | Q-Chat 是问答式，非引导式 | **苏格拉底式教学** 是真 AI 教育差异化 | 后端开发 Socratic Teaching Prompt 模板，前端新增教学模式 UI |
| 4 | **学习进度仪表盘** | P1 | 仅 Quizlet 有基础统计 | **完整的可视化学情分析** 提升家长/学生双重留存 | 后端 Analytics API 聚合学习数据，前端 Compose Canvas 绘制图谱 |
| 5 | **交互式测验** | P1 | Quizlet 核心功能（基于学习集） | AI 自动出题 + 自适应讲解联动 | 后端 Quiz Generation API，前端 QuizScreen 组件 |
| 6 | **间隔重复复习** | P1 | Quizlet 核心功能 | **错题本全来源 + 遗忘曲线算法** 比 Quizlet 更智能 | 后端 Spaced Repetition Engine，前端 ReviewScreen + 本地通知 |
| 7 | **语音交互** | P2 | 所有竞品均缺乏 | **蓝海差异化**，已有基础进一步优化 | 升级 ASR 降噪算法 + 打断优化 |
| 8 | **游戏化** | P2 | Quizlet 强项 | 作为锦上添花，非核心竞争点 | 先实现基础成就/连胜，排行榜后续迭代 |

### 9.5 依赖关系图

```
功能依赖关系（→ 表示"依赖于"）

F40 拍照解题增强 → F21 (拍照基础), F07 (SSE), F09 (Markdown渲染)
F41 自适应分步讲解 → F05 (聊天界面), F07 (SSE), F29 (年级信息)
F42 对话式教学增强 → F05 (聊天界面), F07 (SSE), F43 (仪表盘数据)
F43 学习进度仪表盘 → Room (学习数据), 新增 AnalyticsEngine
F44 交互式测验 → F43 (仪表盘数据), F45 (错题本)
F45 间隔重复复习 → Room (错题数据), 新增 SpacedRepetitionEngine
F46 游戏化 → F43 (仪表盘), Room (统计)
F47 语音交互增强 → F15 (ASR), F20 (语音打断)

层级关系：
    P0               P1                P2
┌──────────┐   ┌──────────────┐   ┌──────────┐
│ F40 拍照  │──▶│ F44 测验     │   │ F46 游戏 │
│ F41 自适应 │   │ F45 间隔重复 │   │ F47 语音 │
│ F42 教学  │──▶│ F43 仪表盘   │   └──────────┘
└──────────┘   └──────────────┘
```

### 9.6 资源估算与排期建议

| 阶段 | 功能 | 预估工时 | 涉及角色 | 备注 |
|------|------|---------|---------|------|
| **Phase 1 (P0)** | F40 拍照解题增强 | 5-8 人日 | 前端 + 后端 | 后端多学科 AI 链路 + 前端 CameraX/OCR 升级 |
| **Phase 1 (P0)** | F41 自适应分步讲解 | 3-5 人日 | 后端为主 + 前端 | 前端步骤折叠组件 + 后端 Prompt 模板 |
| **Phase 1 (P0)** | F42 苏格拉底式教学 | 4-6 人日 | 后端为主 + 前端 | 后端 Socratic Prompt + 前端教学模式切换 UI |
| **Phase 2 (P1)** | F43 进度仪表盘 | 5-8 人日 | 前端 + 后端 | 前端 Compose Canvas + 后端 Analytics API |
| **Phase 2 (P1)** | F44 交互式测验 | 5-7 人日 | 前端 + 后端 | 后端 Quiz Gen API + 前端 QuizScreen |
| **Phase 2 (P1)** | F45 间隔重复复习 | 4-6 人日 | 后端为主 + 前端 | 后端 SM-2 算法 + 前端 ReviewScreen + 通知 |
| **Phase 3 (P2)** | F46 游戏化 | 4-6 人日 | 前端 + 后端 | 成就引擎 + 排行榜 API + 前端动画 |
| **Phase 3 (P2)** | F47 语音交互增强 | 3-5 人日 | 前端 | ASR 降噪 + 打断优化 |

> **说明**：以上估时为纯开发工时，不含测试、Code Review、Bug Fix。P0 阶段建议并行开发 F40/F41/F42（3 个功能可独立开发），预估总计 12-19 人日，约 3-4 周（含测试）。

---

## 10. v2.0 功能迭代（AI Agent + 学习报告导出 + 多语言支持）

> 迭代版本：v2.0
> 基准版本：v1.0（F01-F47 已完成）
> 目标：从"对话式 AI"升级为"Agent 式 AI"，新增学习报告导出与多语言支持

### 10.1 功能列表

| ID | 功能名称 | 优先级 | 概述 |
|----|---------|:------:|------|
| **F48** | AI Agent 联网搜索 | **P0** | 对话中触发 AI 搜索互联网获取最新信息，Agent 思考过程可视化 |
| **F49** | 工具调用框架（计算器/数据查询） | **P0** | 支持多工具注册与调用，前端展示工具调用过程和结果 |
| **F50** | 学习报告页面与 PDF 导出 | **P0** | 学习统计数据概览页面，生成精美排版 PDF 并支持分享 |
| **F51** | 多语言支持（英文界面） | **P1** | 完整国际化方案，英文/中文界面切换，AI 回复语言适配 |

### 10.2 新增功能详细描述

#### P0 功能

---

##### F48 — AI Agent 联网搜索

| 属性 | 内容 |
|------|------|
| **ID** | F48 |
| **优先级** | P0（核心差异化） |
| **功能名称** | AI Agent 联网搜索 |
| **描述** | 用户在对话中可触发 AI 搜索互联网获取最新信息（如当前新闻、最新研究、实时数据）。AI 根据问题自动判断是否需要搜索，用户也可主动要求"联网搜索 xx"。Agent 思考过程前端可视化呈现：思考中... → 搜索中... → 找到结果 → 继续回答。新增 Agent 模式切换开关，用户可在"对话模式"和"Agent 模式"之间切换。后端通过 new-api 的 Tool Calling 能力集成 web_search 工具，支持配置多个搜索引擎源。 |
| **依赖** | F05 (聊天界面), F06 (文本消息发送), F07 (SSE 流式输出), F09 (Markdown 渲染), F29 (用户资料), 后端 Tool Calling API (new-api), 搜索引擎 API 配置 |
| **交互流程** | 见 10.3 节 AI Agent 联网搜索交互流程 |
| **验收标准** | AC58: Agent 思考过程可视化（思考中→搜索中→找到结果→继续回答）；AC59: Agent 模式开关可在设置/聊天页切换；AC60: 联网搜索可正确返回最新信息（测试用例：搜索"今天日期"返回当天日期）；AC61: 用户可主动触发"联网搜索 xx"；AC62: AI 自动判断需搜索时自动触发（如实时数据类问题）；PERF10: 联网搜索端到端耗时≤8s |

---

##### F49 — 工具调用框架

| 属性 | 内容 |
|------|------|
| **ID** | F49 |
| **优先级** | P0（Agent 基础设施） |
| **功能名称** | 工具调用框架（计算器/数据查询） |
| **描述** | 建立可扩展的工具调用框架，支持多种工具的统一注册、调度与结果反馈。首发内置工具：计算器（精确数学运算）、数据查询（单位换算/日期计算等）。框架设计支持后续按需扩展新工具。前端展示工具调用过程和结果（工具名称、输入参数、输出结果）。工具调用结果以结构化消息卡片形式嵌入聊天流中。后端通过 new-api 的 Tool Calling 能力进行工具路由和编排。 |
| **依赖** | F48 (Agent 联网搜索 - 共享 Tool Calling 架构), F05 (聊天界面), F07 (SSE 流式输出), 后端 Tool Registry |
| **交互流程** | 用户提问 → AI 判断需调用工具 → 前端显示"调用工具：计算器/数据查询" → 后端执行工具 → 返回结果 → AI 整合结果继续回答 → 前端显示工具调用卡片（输入/输出） |
| **验收标准** | AC63: 计算器计算 `(23+45)×12` 返回正确结果 816；AC64: 单位换算"1 英里等于多少公里"返回 1.60934；AC65: 工具调用过程在前端卡片展示（工具名+参数+结果）；AC66: 工具调用失败时有降级提示；AC67: 新工具注册无需修改前端核心逻辑 |

---

##### F50 — 学习报告页面与 PDF 导出

| 属性 | 内容 |
|------|------|
| **ID** | F50 |
| **优先级** | P0（用户留存与分享裂变） |
| **功能名称** | 学习报告页面与 PDF 导出 |
| **描述** | 在个人中心/学习仪表盘新增"学习报告"入口，生成可视化的学习数据报告。报告内容包含：学习时长统计（今日/本周/本月）、解题数量与正确率（分学科）、知识点掌握度概览（雷达图/柱状图）、错题汇总（高频错题 TOP5/按学科分类）、学习趋势（近 7 天/30 天曲线）。支持生成精美排版 PDF 报告（Android 原生 PdfDocument API），并分享到系统分享面板（微信/邮件/系统文件）。支持周报/月报两个时间维度模板。用户可设置自动生成周期（每周一/每月 1 号）。 |
| **依赖** | F43 (学习进度仪表盘 - 数据源), Room (解题/错题/学习时长数据), PdfDocument API, 系统分享 (Intent.ACTION_SEND), NotificationManager (定时生成通知) |
| **交互流程** | 见 10.4 节学习报告导出交互流程 |
| **验收标准** | AC68: 学习报告页面展示完整统计（时长、解题、掌握度、错题、趋势）；AC69: PDF 生成成功且排版正确（无乱码/截断/错位）；AC70: PDF 可正常分享到微信/邮件/系统文件；AC71: 支持周报/月报模板切换；AC72: 报告数据与仪表盘数据一致；AC73: PDF 文件大小≤5MB（20 页以内）；PERF11: PDF 生成耗时≤3s |

---

#### P1 功能

---

##### F51 — 多语言支持（英文界面）

| 属性 | 内容 |
|------|------|
| **ID** | F51 |
| **优先级** | P1（国际化基础能力） |
| **功能名称** | 多语言支持 — 英文界面切换 |
| **描述** | 实现完整的 Android strings.xml 国际化方案。将目前硬编码在 Compose 组件和 Activity/Fragment 中的所有 UI 字符串提取到 `res/values/strings.xml`（中文）和 `res/values-en/strings.xml`（英文）。在设置页面新增语言切换选项（中文/English），切换后立即生效（无需重启 App）。AI 回复内容根据用户当前语言设置生成对应语言版本（后端 API 传递 `lang` 参数）。提供可扩展的国际化架构，后续新增语言（如日语/韩语）只需添加 strings.xml 文件即可。 |
| **依赖** | Compose Resource IDs, DataStore (语言偏好存储), 后端 ChatApi (lang 参数), 设置页面重构 |
| **交互流程** | 进入设置 → 语言选择 → 选择 English → 界面即时刷新为英文 → 后续 AI 回复自动以英文生成 |
| **验收标准** | AC74: 所有静态 UI 文案在主界面/设置/聊天/仪表盘/个人中心均有英文翻译；AC75: 切换语言后即时生效，无需重启；AC76: AI 回复语言跟随设置（中文→中文回答，English→English 回答）；AC77: 数据实体（错题/学习记录等）不受语言切换影响；AC78: 新增语言只需添加 strings.xml 文件 |

---

### 10.3 AI Agent 联网搜索交互流程

```
用户对话模式（默认）:
┌─────────────────────────────────────────────────────────────────────┐
│  ChatScreen                                                         │
│                                                                     │
│  [Agent 模式: 关闭] ● 聊天模式                                      │
│                                                                     │
│  用户: 今天有什么新闻?                                              │
│  AI: [思考中...] → [搜索中...] → [找到结果]                        │
│  ┌─────────────────────────────────────────────┐                   │
│  │ ● 搜索来源: Web Search                      │                   │
│  │ ● 关键词: 今日要闻 2026-05-16               │                   │
│  │ └ 找到 3 个结果                              │                   │
│  ├─────────────────────────────────────────────┤                   │
│  │ 📰 根据搜索结果，今天的主要新闻有:            │                   │
│  │ 1. ...                                      │                   │
│  │ 2. ...                                      │                   │
│  │ 3. ...                                      │                   │
│  └─────────────────────────────────────────────┘                   │
│                                                                     │
│  用户: 不用搜索，告诉我勾股定理                                   │
│  AI: 勾股定理是... (直接回答，不触发搜索)                          │
│                                                                     │
│  [输入框] 联网搜索: [用户可主动输入"联网搜索 xxx"]                │
└─────────────────────────────────────────────────────────────────────┘

Agent 模式（手动切换）:
┌─────────────────────────────────────────────────────────────────────┐
│  ChatScreen                                                         │
│                                                                     │
│  [Agent 模式: 开启] ● 每次对话 AI 自动判断是否需要工具             │
│  ┌──────────────────────┐  ┌──────────────────────┐                │
│  │ 🤔 AI 思考中...      │  │ 🔧 调用工具: 计算器  │                │
│  │ 判断: 需要计算       │  │ 输入: (23+45)×12     │                │
│  └──────────────────────┘  └──────────┬───────────┘                │
│                                       │                            │
│  ┌──────────────────────┐  ┌──────────┴───────────┐                │
│  │ ✅ 计算结果: 816      │  │ 📝 AI 正在整合回答... │               │
│  │ 工具: 计算器          │  └──────────────────────┘                │
│  │ 耗时: 0.3s           │                                         │
│  └──────────────────────┘                                         │
│                                                                     │
│  设置入口: 聊天页面右上角 ⋮ → Agent 设置 → 开启/关闭 Agent 模式   │
└─────────────────────────────────────────────────────────────────────┘
```

### 10.4 学习报告导出交互流程

```
┌─────────────────────────────────────────────────────────────────────┐
│  个人中心 → 学习报告                                                  │
│                                                                     │
│  ┌─────────────────────────────────────────────┐                    │
│  │  📊 学习报告 — 本周 (2026-05-11 ~ 05-17)    │                    │
│  │                                            │                    │
│  │  ├── 学习总时长: 12.5 小时 (+15% vs 上周)    │                    │
│  │  ├── 解题总数: 168 题 ✅ 正确率 82.7%       │                    │
│  │  ├── 掌握度概览: [雷达图]                    │                    │
│  │  │   数学 ████████░░ 82%                    │                    │
│  │  │   物理 ██████░░░░ 65%                    │                    │
│  │  │   化学 ███████░░░ 71%                    │                    │
│  │  │   英语 █████████░ 90%                    │                    │
│  │  ├── 高频错题 TOP5:                         │                    │
│  │  │   ① 三角函数诱导公式 (数学, 错 6 次)      │                    │
│  │  │   ② 牛顿第二定律应用 (物理, 错 4 次)      │                    │
│  │  │   ③ ...                                 │                    │
│  │  └── 学习趋势: [折线图] 近 7 天解题量曲线     │                    │
│  │                                            │                    │
│  │  模板: [📅 周报] [📅 月报]                   │                    │
│  │                                            │                    │
│  │  ┌──────────┐  ┌──────────┐                 │                    │
│  │  │ 📄 导出 PDF│  │ 📤 分享   │                 │                    │
│  │  └──────────┘  └──────────┘                 │                    │
│  └─────────────────────────────────────────────┘                    │
│                                                                     │
│  导出流程:                                                            │
│  点击"导出 PDF" → 显示 loading → 生成 PDF → 弹出分享面板              │
│  → 可选择: 微信 / 邮件 / 保存到文件 / 其他应用                         │
│                                                                     │
│  定时报告:                                                           │
│  设置 → 学习报告 → 自动生成: [每周一] [每月 1 号]                     │
│  → 到达时间 → 生成 PDF → 通知"你的学习报告已生成"                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 10.5 依赖关系图 (v2.0)

```
v2.0 功能依赖关系（→ 表示"依赖于"）

F48 AI Agent 联网搜索 → F05 (聊天界面), F07 (SSE), F09 (Markdown)
F49 工具调用框架 → F48 (共享 Tool Calling 架构), F05, F07
F50 学习报告导出 → F43 (仪表盘数据源), Room (学习/错题数据)
F51 多语言支持 → 所有 UI 页面 (strings.xml 迁移), Settings Screen (切换入口)

层级关系:
    P0                        P1
┌─────────────────┐     ┌─────────────────┐
│  F48 Agent搜索  │──┬──│  F51 多语言支持  │
└─────────────────┘  │  └─────────────────┘
┌─────────────────┐  │
│  F49 工具框架    │──┤  （无互依赖）
└─────────────────┘  │
┌─────────────────┐  │
│  F50 学习报告    │──┘
└─────────────────┘

v2.0 与 v1.x 依赖关系:
F48/F49 → new-api (后端 Tool Calling 能力)
F50 → F43 (学习仪表盘), Room DB
F51 → 所有 UI 组件
```

### 10.6 资源估算与排期建议

| 阶段 | 功能 | 预估工时 | 涉及角色 | 备注 |
|------|------|---------|---------|------|
| **Phase 4 (v2.0-P0)** | F48 AI Agent 联网搜索 | 7-9 人日 | 前端 4 + 后端 3-5 | Agent 思考可视化(2-3人日) + 后端 Tool Calling(2-3人日) + 模式切换UI(1人日) + 联调(2人日) |
| **Phase 4 (v2.0-P0)** | F49 工具调用框架 | 5-7 人日 | 前端 2-3 + 后端 3-4 | 工具注册框架(2人日) + 计算器/单位换算工具(2人日) + 前端工具卡片(1-2人日) + 联调(1人日) |
| **Phase 4 (v2.0-P0)** | F50 学习报告导出 PDF | 8-10 人日 | 前端 6-7 + 后端 2-3 | 报告页面(2-3人日) + PDF 生成(2-3人日) + 后端聚合API(2人日) + 分享集成(1人日) + 定时报告(1人日) |
| **Phase 5 (v2.0-P1)** | F51 多语言支持 | 4-6 人日 | 前端 | strings.xml 提取与翻译(2-3人日) + 语言切换UI+逻辑(1-2人日) + AI语言适配(1人日) |

> **排期建议**：
> - **Phase 4（v2.0-P0）**：F48 和 F49 共享 Tool Calling 后端能力，建议 F48 先行启动（后端 Tool Calling 与前端 Agent UI 并行），F49 在 F48 后端 Tool Calling 完成后再启动工具扩展；F50 与 F48/F49 无技术依赖，可与 F48 并行开发。Phase 4 总计 20-26 人日，约 4-5 周（含测试）。
> - **Phase 5（v2.0-P1）**：F51 多语言支持涉及全页面 strings.xml 提取，可在 Phase 4 后端工作空闲时启动，总计 4-6 人日，约 1-2 周。

### 10.7 版本历史

| 版本 | 日期 | 变更内容 | 负责人 |
|------|------|---------|--------|
| v2.0-alpha | 2026-05-16 | 新增第 10 章 v2.0 PRD：F48 AI Agent 联网搜索、F49 工具调用框架、F50 学习报告导出 PDF、F51 多语言支持 | PM |

# AI 学伴后端依赖
# Python 3.11+

# FastAPI 框架
fastapi==0.115.6
uvicorn[standard]==0.34.0

# 数据库
sqlalchemy==2.0.36
aiosqlite==0.20.0
asyncpg==0.30.0   # Docker 部署 PostgreSQL 使用

# Pydantic
pydantic==2.10.3
pydantic-settings==2.7.0

# HTTP 客户端（调用 new-api）
httpx==0.28.1

# TTS — Edge TTS (通过 WebSocket 调用微软 Edge TTS 服务)
websockets==13.1

# 报表 — PDF 生成 (ReportLab) + 图表 (Matplotlib)
reportlab==4.2.5
matplotlib==3.9.3

# 认证
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.19

# OCR (可选 — 安装 PaddleOCR 需要先安装 paddlepaddle)
# paddlepaddle==3.0.0rc1
# paddleocr==2.9.1

# 工具
python-dotenv==1.0.1
alembic==1.14.0

# 测试
pytest==8.3.4
pytest-asyncio==0.24.0
